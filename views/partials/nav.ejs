<nav class="navbar navbar-expand-lg mb-3 p-3 text-white bg-success">
  <div
    class="container-fluid d-flex justify-content-between align-items-center"
  >
    <div class="navbar-brand">
      <img src="/images/IvyTech_Traditional_Vertical_Color.jpg" id="nav-img" alt="">
      <a href="/" class="text-white text-decoration-none fs-4"
        >College Courses</a
      >
    </div>
    <ul class="navbar-nav d-flex flex-row">
      <!-- Check if user is logged in -->
      <% if (user) { %>
      <li class="nav-item me-3">
        <span class="text-white">Welcome, <%= user.email %></span>
      </li>
      <li class="nav-item me-3">
        <a href="/logout" class="btn btn-outline-light">Log Out</a>
      </li>
      <!-- Check if the user is a teacher -->
      <% if (user.isTeacher) { %>
      <li class="nav-item me-3">
        <a href="/addcourse" class="btn btn-outline-light">Add Course</a>
      </li>
      <% } else { %>
      <!-- If user is not a teacher, show the View Schedule link -->
      <li class="nav-item me-3">
        <a href="/schedule" class="btn btn-outline-light">View Schedule</a>
      </li>
      <% } %> <% } else { %>
      <!-- User is not logged in -->
      <li class="nav-item me-3">
        <a href="/login" class="btn btn-outline-light">Log In</a>
      </li>
      <li class="nav-item me-3">
        <a href="/signup" class="btn btn-outline-light">Sign Up</a>
      </li>
      <% } %>
      <li class="nav-item me-3">
        <a href="/allcourses" class="btn btn-outline-light">All Courses</a>
      </li>
    </ul>
  </div>
</nav>
